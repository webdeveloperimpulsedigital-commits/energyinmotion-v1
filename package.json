{
  "name": "energyinmotion",
  "version": "1.0.0",
  "description": "Local development server for Energy In Motion website",
  "main": "index.html",
  "scripts": {
    "start": "npx live-server --port=8080 --host=127.0.0.1",
    "dev": "npx live-server --port=8080 --host=127.0.0.1"
  },
  "dependencies": {
    "live-server": "^1.2.2",
    "pdf-parse": "^2.4.5"
  }
}
